# fstrim plugins

`FilesystemTrimmer` interface — non-fatal trim before unmount.

| Key | Package | Description |
|-----|---------|-------------|
| `default` | default/ | Mount filesystem and run `fstrim` CLI |
